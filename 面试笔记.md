 



# 主从

## 主从同步

1.全量同步：从发replicationId ，主判断是否一致，否则发送主relpicationId , offest 给从同步，再baseve，把RDB发给从，从清空再加载RDB，再执行baseve的期间所执行的命令记录到repl_log，发送给从，从执行发送过来的命令

2.增量同步：同样判断是否replicationId是否一致，以及后续，再从repl_backlog获取offset后面的数据，再发送给从执行命令

```yaml
spring:
  redis:
    sentinel:
      master: mymaster
      nodes:
        - 192.168.150.101:27001
        - 192.168.150.101:27002
        - 192.168.150.101:27003

    lettuce:
      read-from: replica-preferred

```

## 哨兵模式：

监视（1.主管下线，2.客观下线），服务恢复(1.优先权，选小，2.offset值大小，选大，3.id大小，选小) ，脑裂现象

## 分片集群：

有多个集群，每个集群保存不同的数据，每个集群互相监视，客户端可以访问任意的集群，最终都会转发到对应得集群，

集群使用哈希槽来判断该访问哪一个集群

I/O复用，redis使用epoll



# mysql：

## 1.查询解决慢sql：

方式：skyworing , 慢日志查询

 使用explain/desc + select语句

 b+树

聚簇索引和非聚簇索引（二级索引），回表现象，覆盖索引，深分页用聚集索引加子查询，联合索引（最左前缀法则），索引失效的情况

##  2.事务：

1.脏读： 一个事务读到了另一个事务没有提交的数据

2.不可重复读：一个事务先后读取同一条记录，但两次读取的数据不同，称为不可重复读

3.幻读： 一个事务按条件读取数据时没有对应的数据，但是在插入的时候又有对应的数据，称之为幻读

redo_log：用来做持久化的，redo log buffer 把内存中数据页变化的记录wal到磁盘的redo log file中

 undu_log ：提供回滚和mvcc

mvcc原理：

​	当前读 ： 读取记录的最新版本，读取时还要保证其它的并发事务不能修改当前的记录。如：select 。。。 lock in share mode（共享锁），select 。。。for update ，insert，delete等。

​	快照读：简单的select 就是快照读，读取的是记录数据的可见版本，有可能是历史版本，不加锁，非阻塞

​	有两个级别： RC 和 RR，RC（Read Commit）：每次select 都生成一个快照读，RC（Repeatble Read）：开启事务后第一个select 	语句才是快照读的地方

​		readview（读视图）：快照读sql执行的依据，记录并维护当前系统活跃事务的id,

​		包含四个核心字段，m_ids：当前活跃的事务id集合，min_trx_id ：最小活跃事务，max_trx_id：最大事务id+1，预分配事务，creator_trx_id : readview创建者的事务id

​	

# spring 

1.bean是否为线程安全的：

​	否，bean默认是单例的，一般注入的是无状态的单例，这没有线程安全问题，但如果bean定义了变量，此时不是线程安全的，可以用多例和锁来解决

使用@Scope("prototype")和 ObjectProvider<UiidContanier> provider;实现多例

```java

@Component
@Scope("prototype")
public class UiidContanier {
    private final String uuid = UUID.randomUUID().toString();
    public String getUuid() {
        return uuid;
    }
}


@SpringBootTest
class FakeQApplicationTests {
    @Autowired
    ObjectProvider<UiidContanier> provider;

    @Test
    void contextLoads() {
        for (int i = 0; i < 10; i++) {
            System.out.println(provider.getObject().getUuid());
        }

    }

}


```

2.AOP

面向切面编程

1.切面类

```java
@Componet
@Aspect
publica class  TestAspect(){
    @Before('excution(com.ioc.funcations.*(..)')
	public void before(JoinPoint joinpoint){
		String name =	joinpoint.getsignature().getName();	
        List argslist =  joinpoint.getArgs();
	}
    @AfterReturing(value='excution(com.ioc.funcaitons.*(..))',returning = 'result')
    public void afterReturning(JoinPoint joinpoint,Object result){
        String name = joinpoint.getSignature().getName();
		SOUT(name+"的结果为："+result);
    }
    
}

//---------》正式方式用这个

@Component
@Aspect
@Slf4j
public class LogAop {
    @Pointcut("@annotation(com.web.fakeq.annotion.Log)")//这里写注解放在哪里的包下
    public void pointcut() {

    }
    @Around("pointcut()")
    public void around(ProceedingJoinPoint joinPoint) throws Throwable {
        long startTime = System.currentTimeMillis();
        joinPoint.proceed();
        long endTime = System.currentTimeMillis();
        log.info("该方法耗时：{}",(endTime - startTime));
        MethodSignature signature = (MethodSignature) joinPoint.getSignature();
        String name = signature.getMethod().getName();
        Object proceed = joinPoint.proceed();
        Log annotation = signature.getMethod().getAnnotation(Log.class);
        log.info("方法名为{},返回的数据为:{},注解名为：{}",name,proceed,annotation);
    }
}


```

2.实现类



3.IOC

​	控制反转，IOC自动创建对象，交给spring自动创建

​	创建ioc：

​		（1）xml：

​		（2）注解实现

```java
@Data
public class DataConfig(){
    private String name;
    private String localhost;
}

@configration()
public class BeanConfigraton(){
    @Bean(value="config")
	public DataConfig getDataConfig(){
        DataConfig bean =  new DataConfig;
        bean.setName("A1");
        bean.setLocalHost("https://www.host.com")
    	return bean;
	}
}


private void test(){
    ApplicationContext content = new AnnoationConfigApplicationContext(BeanConfigration.class);
    content.getBean("getDataConfig")
    //或者
    content.getBean("config")
}
```

​		（3）扫包加注解

```java
//com.iocstu.proj下
@Data
@Componet //加这个注解表示注入到spring ioc里
public class DataConfig(){
    @Value("A1") // 赋值
    private String name;
    @Value("https://www.host.com")
    private String localhost;
}


private void test(){
    ApplicationContext content = new AnnoationConfigApplicationContext("com.iocstu.proj");//扫包
    content.getBean("DataConfig.class")
 
}
```

## 事务

### 1.事务失效的情况：

（1）事务捕获到异常才能回滚处理，目标自己处理了就不行，可以用throw new RuntimeException(e)抛出

（2）抛出的是检查异常，可以设置@Transcation(rollback=Exception.class)

（3）非 public方法，代理的创建必须是public

### Bean的生命周期：

（1）实列化，用构造函数，

（2）属性注入 @Autowried

（3) Aware 三个接口（NameAware，FactoryAware，ApplicationAware）

（4）初始化

初始化前

postProcessBeforeInitialization

初始化

@PostContrustor

初始化后

postProcessAfterInitialization

（5）销毁Bean

@PreDestroy

### 循环依赖（A注入B，B注入A）：

普通对象：

A实例化后要属性注入B，此时A的原始对象放到earliySingletonObjects（二级缓冲），B进行实例化，属性注入要A，把二级缓存的A原始对象注入，B初始完后放到singletonObjects（一级缓存）中，A再从一级缓存中获取B属性注入

代理对象：

A实例化后要属性注入B，此时A将代理对象工厂放到singletonFactories（三级缓存），B进行实例化，属性注入要A，把三级的A的factory用来生成代理对象A，代理对象A放到二级缓存，B再把代理对象注入，生成了B的Bean，放入到singletonObjects，A再从singletonObjects取出B对象注入，生成的代理对象放入到singletonObjects

### 自动配置原理：

@SpringBootApplicaton

包含{

@SpringBootConfigratioin

@EnableAutoConfigration

@ComponentScan

}

### mybaits：

SqlSessionFactory 到SqlSession到excutor到MappedStatement 

一级缓存，二级缓存 

## 微服务

springcloud 五大组件：注册中心（nacos），负载均衡（Ribbon），远程调用（Feign），服务保护（sentinel），服务网关（GetWay）

### 服务注册：

nacos

### MQ：

消息转换器

```java
  @Bean
    public MessageConverter messageConverter() {
        return new Jackson2JsonMessageConverter();
    }
```

#### 发送者确认机制：

```yaml
publisher-confirm-type: correlated
publisher-returns: true
```

```java
CorrelationData cd = new CorrelationData();
cd.getFuture().addCallback(new ListenableFutureCallback<CorrelationData.Confirm>() {

    @Override
    public void onSuccess(CorrelationData.Confirm result) {
        if(result.isAck()){
            log.info("发送成功");
        }else {
            log.error("发送失败");
        }
    }

    @Override
    public void onFailure(Throwable ex) {
        log.error("服务器失效");
    }
});
HashMap<String, String> map = new HashMap<>();
map.put("name", "A1");
map.put("age", "12");
rabbitTemplate.convertAndSend("amp.gy.direct", "saga", map,cd);
```

```java
@Slf4j
@Configuration
@RequiredArgsConstructor
public class MqConfig {
    private final RabbitTemplate rabbitTemplate;
    @PostConstruct
    public void init() {
        rabbitTemplate.setReturnsCallback(returnedMessage -> {
            log.info("在监听收消息");
            log.info(returnedMessage.toString());
            log.info(returnedMessage.getExchange());
        });
    }
}
```

#### 消费者确认机制：

开启确认机制：

```yaml
    listener:
      simple:
        acknowledge-mode: auto
        retry:
          enabled: true // 开启重试
          initial-interval: 1000 //初始重试时间
          multiplier: 1  //重试倍数
          max-attempts: 3 //重试次数
          stateless: true //有事务就是false 没就是true
        prefetch: 1
```

重试次数用完后就放到错误队列：

```java
@Configuration
public class ErrorMessageQueue {
    @Bean
    public MessageRecoverer republish(RabbitTemplate rabbitTemplate) {
        return new RepublishMessageRecoverer(rabbitTemplate,"errorExchange","error");
    }
}
```

#### lazyqueue：

消息先IO到磁盘，要的时候再读取到内存，3.12以后都是lazyqueue

#### 数据持久化：

默认都是持久化，即使宕机了也能恢复

#### 错误队列：

```java
@Configuration
public class ErrorMessageQueue {
    @Bean
    public MessageRecoverer republish(RabbitTemplate rabbitTemplate) {
        return new RepublishMessageRecoverer(rabbitTemplate,"errorExchange","error");
    }
}

```

#### 幂等性：

```java
    @Bean
    public MessageConverter messageConverter() {
        Jackson2JsonMessageConverter jackson2JsonMessageConverter = new Jackson2JsonMessageConverter();
        jackson2JsonMessageConverter.setCreateMessageIds(true); //消息ID
        return jackson2JsonMessageConverter;
    }
```

#### 死信交换机：

```java
注解配置:
@Configuration
public class DlxFrontQueue {
    @Bean
    public DirectExchange directExchange() {
        return new DirectExchange("dlx.front.exchange");
    }

    @Bean
    public Queue frontQueue() {
        return QueueBuilder.durable("dlx.front.queue").deadLetterExchange("dlx.exchange").build();
    }

    @Bean
    public Binding frontBinding(Queue frontQueue, DirectExchange directExchange) {
        return BindingBuilder.bind(frontQueue).to(directExchange).with("dlx");
    }
}

lisenter绑定:
 @RabbitListener(bindings =
 @QueueBinding(value = @Queue(name = "dlx.queue"),
            exchange = @Exchange(name = "dlx.exchange", type = ExchangeTypes.DIRECT),
            key = {"dlx"}))
	 public void deadExchange(HashMap<String, Long> map) {
        log.info("消息出来:{}",map.toString());
  	 }

消息发送：
 @Test
    void testSend() {
        HashMap<String, Long> map = new HashMap<>();
        map.put("id", 2814569378480130L);
        rabbitTemplate.convertAndSend("dlx.front.exchange", "dlx", map, message -> {
            message.getMessageProperties().setExpiration("10000");
            return message;
        });
        log.info("消息已经发送");
    }
```

dlx.front.exchange --- dlx.front.queue===dlx.exchange---dlx.queue

中间的key要用同一个

#### delay：

```java
注解：
@RabbitListener(bindings = @QueueBinding(
            value = @Queue(name = "delay.queue"),
            exchange = @Exchange(value = "delay.direct",delayed = "true"),
            key = {"delay"}
    ))
    public void delayExchange(HashMap<String, Long> map) {
        log.info("消息获得:{}",map.toString());
    }
消息发送
  @Test
    void testSend2() {
        HashMap<String, Long> map = new HashMap<>();
        map.put("id", 2814569378480130L);
        rabbitTemplate.convertAndSend("delay.direct", "delay", map, message -> {
            message.getMessageProperties().setDelay(5000);
            return message;
        });
        log.info("消息已经发送");
    }

```

# juc



## 线程安全集合类：

### ArrayList , HashSet, HashMap 线程不安全

#### CopyOnWriteArrayList,CopyOnWriteArraySet,ConcurrentHashMap ,写时复制技术解决

> ```java
> class ArrayListClass{
>     List<String> list = new CopyOnWriteArrayList<>();
>     CopyOnWriteArraySet<String> set =  new CopyOnWriteArraySet<String>();
>     ConcurrentHashMap<String, String> map = new ConcurrentHashMap<>();
>     void add(String integer){
>         list.add(integer);
>     }
>     void addSet(String set){
>         this.set.add(set);
>     }
>     void addMap(String key, String value){
>         this.map.put(key,value);
>     }
>     void printSet(){
>         System.out.println(set);
>     }
>     void printHashMap(){
>         System.out.println(map);
>     }
>     void print(){
>         System.out.println(list);
>     }
> 
> }
> ```



## synchronized:

### synchronized（重量级锁）原理：

synchronized 给一个对象，该对象的mark word后两位表示为：10 ，其它位指向monitor 地址，monitor 中的owner指向获取锁的线程的锁记录的object reference，

后面再有其它线程则该线程进入monitor的entrylist队列中等待线程释放锁，释放锁后就把owner置为null，唤醒entrylist中阻塞等待的线程 

### synchronized（轻量级锁）原理：

 synchronized 给一个对象，此时在栈帧中创建了一个lock record 记录 ，该记录中有lock record 锁记录以及锁的对象地址object reference， 先让object reference 指向锁对象，并尝试使用cas替换锁对象的mark word ，将mark word 记录存入 锁记录中，如果cas成功，就将对象头中的mark word 就替换成了，锁记录地址和状态00，表示该对象已经加锁

锁升级：

当一个线程申请一个轻量级锁时，cas失败，就会经入锁升级流程，此时锁对象的mark word 就该为指向monitor地址，后两位也改为10，monitor的owner 指向持有锁的线程的object reference ，申请的线程进入entrylist队列

### wait 原理

锁后其它的进程无法执行，只能阻塞等待，直到线程,前提是线程必须拿到锁，之后才可以使用wait（）, 使用wait之后线程就放到monitor的waitset中，等待其它拿到锁的线程使用notifyAll（）// 激活所有在watiset中的线程 或者notify（）// 随机选择一个在waitset中的线程激活，此时会有虚假激活问题，需要用while循环解决

```
synchronized(obj){

}
```

```java
public class testSyc {
    public static int state = 0;
    public static void main(String[] args) {

        Object o = new Object();

        ThreadGet threadGet = new ThreadGet();

        Thread thread = threadGet.getThread("A1", new Runnable() {
            @Override
            public void run() {
                for (int i = 0; i < 50; i++) {
                    synchronized (o) {

                        try {
                            if (state == 0) {
                                o.wait();
                            }
                            System.out.println("A1执行");
                            state = 0;
                            o.notifyAll();

                        } catch (InterruptedException e) {
                            throw new RuntimeException(e);
                        }
                    }
                }
            }
        });
        Thread thread2 = threadGet.getThread("A2", new Runnable() {
            @Override
            public void run() {
                for (int i = 0; i < 50; i++) {
                    synchronized (o) {

                        try {
                            if (state != 0) {
                                o.wait();
                            }
                            System.out.println("A2执行");
                            state = 1;
                            o.notifyAll();

                        } catch (InterruptedException e) {
                            throw new RuntimeException(e);
                        }
                    }
                }


            }
        });
        threadGet.startThread(thread,thread2);
    }
}

```

给方法上synchronized，锁的对象是this

```

class Product{
    private int state = 0;
    public synchronized void A1() throws InterruptedException {
        while (state % 3 != 0){
            this.wait();
        }
        state++;
        System.out.println("A1 run state:"+state);
          this.notifyAll();
    }
    public synchronized void A2() throws InterruptedException {
        while(state % 3 != 1){
            this.wait();
        }
        state ++;
        System.out.println("A2 run state:"+state);
        this.notifyAll();
    }
    public synchronized void A3() throws InterruptedException {
        while(state % 3 != 2){
            this.wait();
        }
        state ++;
        System.out.println("A3 run state:"+state);
        this.notifyAll();
    }
}
```

注意多个进程（三个以上），notifyAll() 会激发所有线程，用

```java
if(state % 3 != 0){
    this.wait();
   }
```

会将不符合的线程激发继续执行，要用

```java
while (state % 3 != 0){
            this.wait();
        }
```

持续检查

## ReentrantLock 用法

1.可重入锁

2.可打断，其它线程可以使用interrupt来打断线程获取锁的状态

3.锁超时（解决哲学家就擦问题），使用tryLock()方法

```java
public class ReentrantLockStu {
    public static void main(String[] args) {
        ChopStick a = new ChopStick("A");
        ChopStick b = new ChopStick("B");
        ChopStick c = new ChopStick("C");
        ChopStick d = new ChopStick("D");
        ChopStick f = new ChopStick("F");
        ReentrantLockTes reentrantLockTes = new ReentrantLockTes(a,b);
        ReentrantLockTes reentrantLockTes1 = new ReentrantLockTes(b,c);
        ReentrantLockTes reentrantLockTes2 = new ReentrantLockTes(c,d);
        ReentrantLockTes reentrantLockTes3 = new ReentrantLockTes(d,a);
        ReentrantLockTes reentrantLockTes4 = new ReentrantLockTes(f,b);
        ThreadGet threadGet = new ThreadGet();
        threadGet.startThread(threadGet.getThread("A",reentrantLockTes),threadGet.getThread("B",reentrantLockTes1),threadGet.getThread("C",reentrantLockTes2),threadGet.getThread("D",reentrantLockTes3),threadGet.getThread("F",reentrantLockTes4));
        System.out.println("主进程开始");
    }
}
class ReentrantLockTes implements Runnable{
    ChopStick left;
    ChopStick right;
    ReentrantLockTes(ChopStick left, ChopStick right){
        this.left = left;
        this.right = right;
    }

    @Override
    public void run() {
       while (true){
           if (left.tryLock()){
               try {
                   if (right.tryLock()){
                       try {
                           System.out.println(Thread.currentThread().getName()+" 开始吃了");
                           Thread.sleep(1000);
                       } catch (InterruptedException e) {
                           throw new RuntimeException(e);
                       } finally {
                           right.unlock();
                       }

                   }
               }finally {
                   left.unlock();
               }
           }
       }
    }
}
class ChopStick extends ReentrantLock {
    String name;
    ChopStick(String name){
        this.name = name;
    }
}
```

4.公平锁（可以忽略）

5.条件变量。先要获取锁才可以进入条件等待，可以指定一个条件等待的waitset 唤醒，不需要唤醒全部的等待线程

```java
class Share{
    private int flag = 3;
    ReentrantLock lock = new ReentrantLock();
    //条件变量
    Condition condition = lock.newCondition();
    Condition condition2 = lock.newCondition();
    Condition condition3 = lock.newCondition();
    public void A1()   {
    	//先要获取锁才可以进入条件等待
        lock.lock();
        try {
            while( flag != 1){
                condition.await();
            }
            flag = 2;
            System.out.println("A1 run");
            //可以指定一个条件等待的waitset 唤醒，这里唤醒在condition2条件中等待的所有线程
            condition2.signalAll();
        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        } finally {
            lock.unlock();
        }

    }
    public void A2()   {
        lock.lock();
        try {
            while( flag != 2){
                condition2.await();
            }
            flag = 3;
            System.out.println("A2 run");
            condition3.signal();
        } catch (Exception e) {
            throw new RuntimeException(e);
        } finally {
            lock.unlock();
        }
    }
    public void A3()   {
        lock.lock();
        try {
            while( flag != 3){
                condition3.await();
            }
            flag = 1;
            System.out.println("A3 run");
            condition.signal();
        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        } finally {
            lock.unlock();
        }
    }
}
```



## JMM

### 1.可见性问题：

```java
public class ReentrantLockStu {
    static boolean run = true;
    public static void main(String[] args) {
        Thread thread = new Thread(new Runnable() {
            @Override
            public void run() {
                for (int i = 0; i < 10000000; i++) {
                  if (run){
                        System.out.println("run");
                    }else{
                        System.out.println("stop");
                        break;
                    }
                }
            }
        });
        thread.start();
        run = false;
    }
}
```

​	JIT自动优化了执行代码，因为子线程要频繁重复地从主存中读取run值，JIT会将run值缓存至自己工作内存的高速缓存中，减少对主存的值的访问，提高效率，但如果之后主线程修改了run的值，那么子线程就无法发现run的值改变依旧运行

解决方案：使用volatile关键字加给静态变量，或者使用synchronized 解决（synchronized工作时会将主存的内容读取到工作缓存中）

### 2 . 有序性问题（指令重排）：

多线程中，指令会重排，造成执行顺序的破坏，可以通过禁用指令重排解决改问题

### 面试单例

```java
//单例模式
// 饿汉式单例，类加载阶段就会初始化单例
//1.实现了序列化接口如和保证单例不被破坏
public final class SingleTonImp implements Serializable {
    //为什么是private，是否能防止反序列化破坏单例
    private SingleTonImp(){};
    private final static SingleTonImp INSTANCE = new SingleTonImp();
    public static SingleTonImp getInstance(){
        return INSTANCE;
    }
    //1.防止反序列化破坏单例
    public Object readResolve(){
        return INSTANCE;
    }
}

//懒汉模式
//单例模式
//1.实现了序列化接口如和保证单例不被破坏
public final class SingleTonImp implements Serializable {
    //为什么是private，是否能防止反序列化破坏单例
    private SingleTonImp(){};
    private static SingleTonImp INSTANCE = null;
    public static synchronized SingleTonImp getInstance(){
       if (INSTANCE == null){
           INSTANCE = new SingleTonImp();
       }
       return INSTANCE;
    }
    //1.防止反序列化破坏单例
    public Object readResolve(){
        return INSTANCE;
    }
}
```

## Callable , FutureTask用法：

```
public class CallableTes {
    public static void main(String[] args) throws ExecutionException, InterruptedException {
        ThreadGet threadGet = new ThreadGet();
        FutureTask futureTask = new FutureTask<>(new Callable<Object>() {
            @Override
            public Object call() throws Exception {
                Thread.sleep(5000);
                System.out.println("FA进程开始:");
                return 2000;
            }
        });
        System.out.println("主进程开始:");
        threadGet.getThread("A1",futureTask).start();
        System.out.println("主进程获得:"+futureTask.get());
    }

}
```

## CountDownLatch 倒计时锁用法：

用来进行线程同步协作，等待所有线程完成倒计时。

构造参数用来初始化等待计数值，await（）用来等待计数归零，countDown（）用来让计数减一

```java
public class CallableTesCountDown {
    public static void main(String[] args) throws InterruptedException {
        CountDownLatch countDownLatch = new CountDownLatch(7); //指定要求7个资源
        ThreadGet threadGet = new ThreadGet();
        for (int i = 0; i < 7; i++) {
            threadGet.getThread("A" + i, new Runnable() {
                @Override
                public void run() {
                    System.out.println(Thread.currentThread().getName()+ "减少资源");
                    countDownLatch.countDown(); //获得一个资源，进程继续，不会等待主进程
                }
            }).start();
        }
        countDownLatch.await(); // 主进程等待，等7个所有资源获得主进程执行
        System.out.println("主进程开始执行");
    }
}

```



## CyclicBarrier 用法：

循环栅栏，用来线程协作，构造时设置计数个数，每个线程执行到某个需要同步的时候调用await方法进行等待，当等待个数等于计数个数时继续执行

```java
  public static void main(String[] args) throws ExecutionException, InterruptedException {
        ThreadGet threadGet = new ThreadGet();
        CyclicBarrier cyclicBarrier = new CyclicBarrier(5,()->{
            System.out.println("进程收集完成，开始执行任务");
        }); //设置收集到5个进程信号开始执行的任务R
        for (int i = 0; i < 5; i++) {
           threadGet.getThread("A"+i, () -> {
               System.out.println("收集到一个线程");
               try {
                   cyclicBarrier.await(); // 任务R收集到一个进程信号，等待任务R执行再继续执行
                   System.out.println(Thread.currentThread().getName()+"开始向下执行");
               } catch (InterruptedException | BrokenBarrierException e) {
                   throw new RuntimeException(e);
               }
           }).start();
        }

    }
```

## 读写锁

ReentrantReadWriteLock

```
class ReadAndWriteLockImp {
    private  HashMap<String, String> map  = new HashMap<>();
    ReentrantReadWriteLock lock = new ReentrantReadWriteLock();

    void write(String key,String value) throws InterruptedException {
        lock.writeLock().lock();

        try {
            System.out.println(Thread.currentThread().getName() + " 开始写");
            map.put(key, value);
            Thread.sleep(1000);
            System.out.println(Thread.currentThread().getName()+"写完了");
        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        } finally {
            lock.writeLock().unlock();
        }
    }
    String read(String key) throws InterruptedException {
        lock.readLock().lock();

        try {
            System.out.println(Thread.currentThread().getName()+"开始读");
            Thread.sleep(1000);
            System.out.println(Thread.currentThread().getName()+"读完了,读到了"+map.get(key));
            return map.get(key);
        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        } finally {
            lock.readLock().unlock();
        }

    }
}

```

## 锁降级

同一个线程可以同时拿到读锁和写锁，边读边写

```java
   void writeAndRead(String key,String value) throws InterruptedException {
        lock.writeLock().lock();
        lock.readLock().lock();
        try {
            //写
            System.out.println(Thread.currentThread().getName() + " 开始写");
            map.put(key, value);
            Thread.sleep(1000);
            System.out.println(Thread.currentThread().getName()+"写完了");
            //读
            System.out.println(Thread.currentThread().getName()+"开始读");
            Thread.sleep(1000);
            System.out.println(Thread.currentThread().getName()+"读完了,读到了"+map.get(key));

        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        } finally {
            lock.writeLock().unlock();
            lock.readLock().unlock();
        }
    }
```

## 阻塞队列

put ,take offer(time),pull(time)

![](C:\Users\goodf\Pictures\Screenshots\屏幕截图 2026-01-25 000407.png)

## 线程池

```java
 public ThreadPoolExecutor(
    //核心线程数量
    int corePoolSize,
    //最大线程数量 
	int maximumPoolSize,
    //针对救急线程的最大存活时间
	long keepAliveTime,
	//时间单位
    TimeUnit unit,
    //阻塞队列
	BlockingQueue<Runnable> workQueue
 	//线程工厂
    defaultThreadFactory
    //拒绝策略
    defaultHandler
 	）
```

### Executors方法创建jdk的线程池

```java
//固定大小的线程池，阻塞队列是likedBlockQueue 无限大小的阻塞队列，可以放任意数量的任务
Executors.newFixedThreadPool
//核心线程数量是0，救急线程数无限创建，存活时间为60秒，队列是SynchronousQueue ， 没有线程取是放不进去的
Executors.newCachedThreadPool
//线程固定为一个，任务多余一个的时候放到无界队列中去
Executors.newSingleThreadExecutor
```

### 提交任务方法：

```java
//execute 方法
executorService.execute(() -> System.out.println("execute 执行"));
//submit 方法
//接受一个 Callable<T> task ，返回一个Future<T> ，利用保护性暂停设计模式
ExecutorService executorService = Executors.newFixedThreadPool(4);
Future<String> submit = executorService.submit(() -> {
    Thread.sleep(5000);
    return "ok";
});
//主线程等待获取结果
System.out.println("主线程执行等待获取结果:");
System.out.println(submit.get());
//invokeAll 方法
// 接受一个 Callable的List ， 返回Future 的List
ExecutorService executorService = Executors.newFixedThreadPool(4);
List<Future<String>> futures = executorService.invokeAll(Arrays.asList(
    () -> {
        Thread.sleep(1000);
        return "1";
    },
    () -> {
        Thread.sleep(500);
        return "2";
    },
    () -> {
        Thread.sleep(2000);
        return "3";
    }
));
futures.forEach(f->{
    try {
        System.out.println(f.get());
    } catch (InterruptedException e) {
        throw new RuntimeException(e);
    } catch (ExecutionException e) {
        throw new RuntimeException(e);
    }
});
//invokeAny 方法 ， 哪个先执行完获取哪个线程的返回值，其它线程停止
ExecutorService executorService = Executors.newFixedThreadPool(4);
String  invokeAny = executorService.invokeAny(Arrays.asList(
    () -> {
        Thread.sleep(1000);
        return "1";
    },
    () -> {
        Thread.sleep(500);
        return "2";
    },
    () -> {
        Thread.sleep(2000);
        return "3";
    }
));
System.out.println(invokeAny);
```

### tomcat

LimitLatch :  负责限流，控制最大连接个数

Acceptor： 只负责接受新socket连接

Poller ： 只负责监听是否有socketchannel 的io事件，一旦有可读io事件，就封装一个任务对象给Executor执行

Executors： 处理请求

## AQS

```java
class MyLock implements Lock {
    private Mysync sync = new Mysync();
    //同步器类
    class Mysync extends AbstractQueuedLongSynchronizer {
        @Override
        protected boolean tryAcquire(long arg) {
            if (compareAndSetState(0,1)){
                setExclusiveOwnerThread(Thread.currentThread());
                return true;
            }
            return false;
        }

        @Override
        protected boolean tryRelease(long arg) {
            setExclusiveOwnerThread(null);
            setState(0);
            return true;
        }

        @Override
        protected boolean isHeldExclusively() {
           return  getState() == 1;
        }
        public Condition newCondition(){
            return new ConditionObject();
        }
    }
    @Override
    public void lock() {
        sync.tryAcquire(1);
    }

    @Override
    public void lockInterruptibly() throws InterruptedException {
        sync.acquireInterruptibly(1);
    }

    @Override
    public boolean tryLock() {
       return sync.tryAcquire(1);
    }

    @Override
    public boolean tryLock(long time, TimeUnit unit) throws InterruptedException {
        return sync.tryAcquireNanos(1,unit.toNanos(time));
    }

    @Override
    public void unlock() {
        sync.release(1);
    }

    @Override
    public Condition newCondition() {
        return sync.newCondition();
    }
}
```

### ReentrantLock 原理

```java
//默认是非公平锁
public ReentrantLock() {
    sync = new NonfairSync();
}
```

#### 加锁过程：

没有竞争的时候直接setExclusiveOwnerThread 设为当前线程，compareAndSetState将state改为1

出现竞争时，CAS失败，进入tryAcquire ，此时state 仍然为1 ，失败，进入addWaiter逻辑，构造Node队列，第一个Node称为：哨兵，后面跟着线程关联的Node，每个Node都有一个waitStatus状态，0为默认正常状态，且Node的创建时懒惰的。

当前线程进入acquireQueued逻辑：

1.acquireQueued 会在一个死循环中不断尝试获得锁，失败后进入park阻塞

2.如果是排在第二位的Node，则tryAcquire 尝试获取锁，此时state为1，失败进入shouldParkAfterFailedAcquire逻辑

3.shouldParkAfterFailedAcquire： 将前驱节点node的waitState改为-1，返回false , 重新进入acquireQueued循环逻辑，如果是排在第二位的Node，tryAcquire 后如果还是失败，进入shouldParkAfterFailedAcquire，此时返回true，并且改线程进入阻塞：parkAndCheckInterrupt

释放锁流程：

1.release 逻辑，设置state 为0 ，setExclusiveOwnerThread 为null，检查head是否为null，不为nul而且waitstate不为0的话就唤醒后继节点：unparkSuccessor（head） ，唤醒后的节点就可以竞争锁了，把setExclusiveOwnerThread 设为唤醒的成功获得锁的进程，设置state为1

2.非公平锁的话会产生竞争，如果竞争失败，重新进入acquireQueuedlasticserach

#### 可重入原理：

第一次进入就设置state为1 ，setExclusiveOwnerThread  为当前线程，之后再次进入就把state++，释放时就state--，再判断state 是否为0，0的话就返回true，setExclusiveOwnerThread  为null

#### 可打断原理：

1.不可打断模式：在AQS队列中其它线程打断时无法打断，只能设置打断标识，只能在获取锁之后继续运行，但没有被打断，只是打断标识为true，在AQS的acquireQueued 逻辑中，在park过程中被interrupt进入打断表示设为true

2.可打断模式：在AQS的acquireQueued 逻辑中，在park过程中被interrupt进入抛出异常逻辑

#### 公平锁的实现：

tryAcquire 的时候会判断AQS队列中是否有前驱节点，没有才会竞争，hasQueuedPredecessors（）：判断头是不是等于尾，不等表示队列中有Node，有Node继续判断队列中有没有老二节点或者老二节点是不是此线程，二者有一个成立就可以返回true，都不成立返回false，没Node直接返回false。

## 创建索引库：

mapping:

提示：一个对象用一个properties表示，再写字段对应的类型，像如下的userName的嵌套对象，type用object再用properties继续编写其包含的字段名称

> ```json
> PUT /chates
> {
>   "mappings": {
>     "properties": {
>       "info":{
>         "type": "text",
>         "analyzer": "ik_smart"
>       },
>       "userName":{
>         "type": "object",
>         "properties": {
>           "firstName":{
>             "type": "keyword"
>           },
>           "secondName":{
>             "type": "keyword"
>           }
>         }
>       }
>     }
>   }
> }
> 
> 
> 
> //可以通过把字段copy去另外一个字段来优化查询
> PUT /hotel
> {
>   "mappings": {
>     "properties": {
>       "id":{
>         "type": "keyword"
>       },
>       "name":{
>         "type": "text",
>         "analyzer": "ik_max_word",
>          "copy_to": "all"
>       },
>       "address":{
>         "type": "text",
>         "index": false
>       },
>       "price":{
>         "type": "integer"
>       },
>       "score":{
>         "type": "integer"
>       },
>       "brand":{
>         "type": "keyword",
>         "copy_to": "all"
>       },
>       "city":{
>         "type": "keyword"
>       },
>       "starName":{
>         "type": "keyword"
>       },
>       "business":{
>         "type": "keyword",
>          "copy_to": "all"
>       },
>       "location":{
>         "type": "geo_point"
>       },
>       "pic":{
>         "type": "keyword",
>         "index": false
>       },
>       "all":{
>         "type": "text",
>         "analyzer": "ik_max_word"
>       } //倒排索引的机制
>     }
>   }
> }
> ```
>

## 索引库的查询，删除 ,增加字段

```json
GET /chates

PUT /chates/_mapping
{
  "properties":{
    "age":{
      "type":"integer"
    }
  }
}

DELETE /chates
```

## 文档的添加，查询，删除

```java
POST /chates/_doc/1
{
  "info":"是一名专业软件开发工程师，目前正在积累经验",
  "userName":{
    "firstName": "高",
    "secondName": "洋"
  },
  "age": 21
}


GET /chates/_doc/1

DELETE /chates/_doc/1
```

## 文档的修改

```json
//全量修改,会将原本的所有内容修改，原本不存在就会新增
PUT /chates/_doc/1 
{
   "info":"是一名专业软件开发工程师，目前正在积累经验",
  "userName":{
    "firstName": "1高",
    "secondName": "1洋"
  },
  "age": 31
}
//增量修改，局部修改，修改指定字段
POST /chates/_update/1
{
  "doc": {
    "age" : 21,
    "userName":{
      "firstName": "高",
      "secondName": "洋"
    }
  }
}
```



# java基础：

## hashmap:

### 存储过程：

1.在jdk8以前，在构造方法中是创建一个长度是16的Entry[] table 用来存储键值对数据，在jdk8以后就不是在构造器方法中创建数组了，只有调用了put方法才创建数组。Node[] table是用来存储键值对数据的。

2.向hash表中存储数据，先用key的hashcode获取哈希值，再用扰动函数将hashcode值无符号右移16位（以32位机为例），再与原来的hashcode进行按位异或的操作得到扰动后的hash值。

```java
//扰动函数
static final int hash(Object key) {
    int h;
    return (key == null) ? 0 : (h = key.hashCode()) ^ (h >>> 16);

```

再把获得hash值与数据长度n减一 按位与运算得到数组下标

```java
(n - 1) & hash
```

再根据下标将键值对放到数组中去

3.如果后面put的数据得到的下标中的位置不为null，即已经有键值对占位，则在此节点上再划分一个节点存储键值对数据

4.hash值是相同的话，发生hash碰撞，则调用String的equals方法来判断两个内容是否相等，相等：则将后添加的数据的value覆盖之前的value，不想等：则继续向下与其它数据的key相比较，如果都不相等，则划出一个空间节点存储数据

### 继承关系

```java
ublic class HashMap<K,V> extends AbstractMap<K,V>
    implements Map<K,V>, Cloneable, Serializable
```

### 成员变量

```java
//序列化版本号
private static final long serialVersionUID = 362498820763181265L;
//默认初始容量
//容量大小必须是2的n次幂
//原因是计算机计算下标的时候是使用(n - 1) & hash ，即length-1 与 hash ，这个前提是length必须是2的n次方幂，
//如果不是2的n次幂，会导致频繁哈希冲突
 static final int DEFAULT_INITIAL_CAPACITY = 1 << 4; // aka 16
//在初始化的时候，如果给的长度不是2的n次方幂，就会算出大于initialCapacity的最小的2的n次方幂的数
public HashMap(int initialCapacity, float loadFactor) {
        if (initialCapacity < 0)
            throw new IllegalArgumentException("Illegal initial capacity: " +
                                               initialCapacity);
        if (initialCapacity > MAXIMUM_CAPACITY)
            initialCapacity = MAXIMUM_CAPACITY;
        if (loadFactor <= 0 || Float.isNaN(loadFactor))
            throw new IllegalArgumentException("Illegal load factor: " +
                                               loadFactor);
        this.loadFactor = loadFactor;
        this.threshold = tableSizeFor(initialCapacity);
    }
static final int tableSizeFor(int cap) {
        int n = -1 >>> Integer.numberOfLeadingZeros(cap - 1); // Integer.numberOfLeadingZeros(cap - 1) 会返回 cap-1 的二进制表示中，从最高位开始连续有多少个 0。
    	
        return (n < 0) ? 1 : (n >= MAXIMUM_CAPACITY) ? MAXIMUM_CAPACITY : n + 1;//如果 n < 0：返回 1（处理 cap <= 1 的极端情况）,如果 n >= MAXIMUM_CAPACITY：返回 MAXIMUM_CAPACITY（HashMap 最大容量，通常是 2³⁰）,否则返回 n + 1
    }
//扩容阈值：
//  threshold =容量*负载因子 ,超过threshold进行扩容
int threshold;
//	负载因子/加载因子
  final float loadFactor;
```

#### 数组长度：

容量大小必须是2的n次幂
原因是计算机计算下标的时候是使用(n - 1) & hash ，即length-1 与 hash ，这个前提是length必须是2的n次方幂，
如果不是2的n次幂，会导致频繁哈希冲突

#### 初始化的时候

在初始化的时候，如果给的长度不是2的n次方幂，就会算出大于initialCapacity的最小的2的n次方幂的数

#### 负载因子/加载因子：

用来计算下一个容量大小，int threshold;  threshold;

#### 转化红黑树的边界值

根据泊松分布算出节点大于8的概率很小，而且treenode的空间占用大小是普通Node的两倍，所以只有当bin包含足够多的节点才会转换成红黑树

数的接点数小于6会变回链

# Redis 原理：

## 1.SDS简单动态字符

如：set name “小王”， 这里的name  和 “小王”都是SDS

一个c语言的结构体

uint8_t  len; 已保存的字符串字节数 //无符号整型8个bit 最大255

uint8_t  alloc; 申请的总字节数 // 如果字符串小于1M，则扩容后alloc 值位新字符串大小*2+1*，但实际上不包含“\0”，所以是新字符串大小2+1-1

unsigned char flags; //对应不同的结构体，有很多的sds，最小5 最大64，flag值1对应8，flag值4对应最大64

char buf[]

## 2.INTSET

一个c语言的结构体

uint32_t encoding; // 编码方式，支持16，32，64位

uint32_t length;//元素个数

int8_t contents []; // 保存集合数据

方便查找，Redis会将intset所有的整数按照升序依次保存在contenes数组中

intset升级：脚标不变，编码方式改变，从后往前进行拷贝扩容

## 3.DICT

由三个部分组成，哈希表（DictHashTable），哈希节点（DictEntry），字典（Dict）

Dic:

```C
typedef struct dict
{
    dicType *type// dict类型，内置不同函数
    void *private;// 私有数据,在特殊hash时使用
    dictht ht[2]; // 一个Dict 包含两哈希表，一个记录当前数据，一个一般时空，rehash时使用
    long rehashidx; // rehash进度，-1表示未进行
    int16_t pauserehash;// rehash是否暂停
};

typedef struct  dictht
{
    dictEntry **table; // entry 数组，数组保存指向entry指针
    unsigned long size; // 哈希表大小
    unsigned long sizemark // 哈希表掩码，等于size - 1
    unsigned long used;//entry 个数
};


typedef struct  dictEntry{
	void *key;//键
    union{
		void *val;
        ...
    } vl // 值
    struct dictEntry *next;// 下一个entry的指针
}

```



### 扩容：

Dict每次新增都会检查负载因子（used/size）大小，负载因子>= 1 并且没有bgsave 或者 bgrewriteaof 等待后台进程 或者 负载因子 > 5 就会触发哈希表扩容

### 收缩：

每次删除元素也会对负载因子检查，当负载因子小于0.1 时，做哈希表收缩

### 渐进式rehash：

1. 判断是扩容还是收缩
2. 如果是收扩容，则新size为第一个大于等于dict.ht[0].used+ 1 的 2 的n次幂，如果是收缩，则新size为第一个小于等于dict.ht[0].used+ 1 的 2 的n次幂
3. 按照新的realeSize申请内存空间，创建dictht ，并赋值给dict.ht[1]
4. 设置dict.rehashidx = 0 ，表示开始rehash
5. 将dict.ht[0]中的每一个dictEntry 都rehash 到 dict.ht[1]
6. 将dict.ht[1] 赋值给dict.ht[0] ，dict.ht[1] 初始化为空,释放原来的dict.ht[0] 的空间

## 4.ZipList

zlbytes|zltail| zllen | entry |.... | entry | zlend |

zlbytes：总字节数

zltail: 尾节点地址

zllen：entry 数量 

zlend：结束标识

## String:

![](C:\Users\goodf\Pictures\Screenshots\Strin.png)



## List:

![](C:\Users\goodf\Pictures\Screenshots\List.png)



## Set:

![](C:\Users\goodf\Pictures\Screenshots\Set.png)



## Zset:

![](C:\Users\goodf\Pictures\Screenshots\Zset.png)



## Hash:

![](C:\Users\goodf\Pictures\Screenshots\Hash.png)

# ElasticSearch:RestClient

## DSL语句：【

### 查询：

```json
查询所有
GET /hotel/_search
{
  "query": {
    "match_all": {}
  }
}

字段查询
GET /hotel/_search
{
  "query": {
    "match": {
      "all": "上海"
    }
  }
}
//精确查询
GET /hotel/_search
{
  "query": {
    "term": {
      "city": {
        "value": "上海"
      }
    }
  }
}
//自定义函数查询
GET /hotel/_search
{
  "query": {
    "function_score": {
      "query": {"match": {
        "all": "上海"
      }},
      "functions": [
        {
          "filter": {
            "term": {
              "id": 1
            }
          },
          "weight": 100
        }
      ],
      "boost_mode": "replace"
    }
  }
}
//排序查询
GET /hotel/_search
{
  "query": {
    "match_all": {}
  },
  "sort": [
    {
      "id": {
        "order": "asc"
      }
    }
  ]
}
```

## RestClient

```、
    //属于批量添加数据，含IndexRequest 文档插入数据
    private void insertToES() {
        BulkRequest bulkRequest = new BulkRequest();
        List<Hotel> hotelList = authMapper.FindHotelList();
        System.out.println(hotelList.size());
        try {
            for (Hotel hotel : hotelList) {
                HotelDoc hotelDoc = new HotelDoc(hotel);
                String jsonString = JSONObject.toJSONString(hotelDoc);
                bulkRequest.add(
                    //indexRequest文档查询
                    new IndexRequest().index("hotel").source(jsonString, XContentType.JSON)
                
                );
            }
            client.bulk(bulkRequest, RequestOptions.DEFAULT);
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }





```

## SearchRequest 文档查询

### match

```java
	//SearchRequest 文档查询
	private void searchInES() {
        try {
            //
            SearchRequest request = new SearchRequest("hotel");
			//muti_match 多字段查询
            //match_all 查询所有
			//match	单字段查询
            request.source().query(QueryBuilders.multiMatchQuery("上海", "city","name"));
            request.source().query(QueryBuilders.matchAllQuery());
            request.source().query(QueryBuilders.matchQuery("all","如家"));
            //
            SearchResponse response = client.search(request, RequestOptions.DEFAULT);
            //hits中有getTotalHits() ，getHits();
            SearchHits hits = response.getHits();
            TotalHits totalHits = hits.getTotalHits();
            log.info("totalHits一共有:{}", totalHits.value);
            SearchHit[] hits1 = hits.getHits();
            for (SearchHit hit : hits1) {
                String json = hit.getSourceAsString();
                HotelDoc hotelDoc = JSONObject.parseObject(json, HotelDoc.class);
                log.info("数据为:{}", hotelDoc);
            }

        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }
    
  
```

### boolean / term / range

```java
    private void searchInES() {
        try {
            SearchRequest request = new SearchRequest("hotel");
            //must
            //filter 
            BoolQueryBuilder boolQueryBuilder = QueryBuilders.boolQuery();
            boolQueryBuilder.must(QueryBuilders.termQuery("city", "上海"));
            boolQueryBuilder.filter(QueryBuilders.rangeQuery("price").lte(250));
            request.source().query(boolQueryBuilder);
            //
            SearchResponse response = client.search(request, RequestOptions.DEFAULT);
            SearchHits hits = response.getHits();
            TotalHits totalHits = hits.getTotalHits();
            log.info("totalHits一共有:{}", totalHits.value);
            SearchHit[] hits1 = hits.getHits();
            for (SearchHit hit : hits1) {
                String json = hit.getSourceAsString();
                HotelDoc hotelDoc = JSONObject.parseObject(json, HotelDoc.class);
                log.info("数据为:{}", hotelDoc);
            }

        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }
```

### 排序和分页

```java
 private void searchInES() {
        try {
            SearchRequest request = new SearchRequest("hotel");
            //sort
            //page
            request.source().query(QueryBuilders.matchAllQuery()).sort("price", SortOrder.ASC);
            request.source().from(0).size(5);
            //
            SearchResponse response = client.search(request, RequestOptions.DEFAULT);
            SearchHits hits = response.getHits();
            TotalHits totalHits = hits.getTotalHits();
            log.info("totalHits一共有:{}", totalHits.value);
            SearchHit[] hits1 = hits.getHits();
            for (SearchHit hit : hits1) {
                String json = hit.getSourceAsString();
                HotelDoc hotelDoc = JSONObject.parseObject(json, HotelDoc.class);
                log.info("数据为:{}", hotelDoc);
            }

        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }
```

### 聚合：

```java
  private void searchInESofBucket() {
        try {
            SearchRequest request = new SearchRequest("hotel");
            //聚合
            request.source().size(0);
            request.source().aggregation(AggregationBuilders.terms("brandAggs").field("brand").size(10));
            //给聚合添加查询字段过滤
            request.source().query(QueryBuilders.matchQuery("city", "上海"))
            //
            SearchResponse response = client.search(request, RequestOptions.DEFAULT);
            //解析response
            Aggregations aggregations = response.getAggregations();
            Terms terms = aggregations.get("brandAggs"); //这里返回的是一个聚合，里面有buckets
            List<? extends Terms.Bucket> buckets = terms.getBuckets(); // 这里获取buckets
            buckets.forEach(bucket -> {
               String key = bucket.getKeyAsString(); //获取buckets的name字段
               resultMap.putIfAbsent(aggName, new ArrayList<>());
               resultMap.get(aggName).add(key);
           	});
           
            //
            System.out.println( response);

        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }
```

### 拼音分词器的用法：

先用ik_max_word 分词，再根据pinyin来创建拼音索引，字段设置分词器的时候，创建时analyzer用 定义的先ik_max_word  再 pinyin 的分词器，但查询的时候用ik_smart 分词器

### 自动补全suggestion：

```json
PUT /hotel
{
  "settings": {
    "analysis": {
      "analyzer": {
        "text_anlyzer": {
          "tokenizer": "ik_max_word",
          "filter": "py"
        },
        "completion_analyzer": {
          "tokenizer": "keyword",
          "filter": "py"
        }
      },
      "filter": {
        "py": {
          "type": "pinyin",
          "keep_full_pinyin": false,
          "keep_joined_full_pinyin": true,
          "keep_original": true,
          "limit_first_letter_length": 16,
          "remove_duplicated_term": true,
          "none_chinese_pinyin_tokenize": false
        }
      }
    }
  },
  "mappings": {
    "properties": {
      "id":{
        "type": "keyword"
      },
      "name":{
        "type": "text",
        "analyzer": "text_anlyzer",
        "search_analyzer": "ik_smart",
        "copy_to": "all"
      },
      "address":{
        "type": "keyword",
        "index": false
      },
      "price":{
        "type": "integer"
      },
      "score":{
        "type": "integer"
      },
      "brand":{
        "type": "keyword",
        "copy_to": "all"
      },
      "city":{
        "type": "keyword"
      },
      "starName":{
        "type": "keyword"
      },
      "business":{
        "type": "keyword",
        "copy_to": "all"
      },
      "location":{
        "type": "geo_point"
      },
      "pic":{
        "type": "keyword",
        "index": false
      },
      "all":{
        "type": "text",
        "analyzer": "text_anlyzer",
        "search_analyzer": "ik_smart"
      },
      "suggestion":{
          "type": "completion",
          "analyzer": "completion_analyzer"
      }
    }
  }
}
```

### 自动补全javaAPI

```java
@Component
public class Suggestion {
    @Autowired
    RestHighLevelClient elasticSearchClient;
    public void searchBuInSuggestion(){
        try {
            SearchRequest request = new SearchRequest("hotel");
            request.source().suggest(new SuggestBuilder().addSuggestion(
                    "suggestions",
                    SuggestBuilders.completionSuggestion("suggestion")
                            .prefix("h")
                            .skipDuplicates( true)
                            .size(10)
            ));
            SearchResponse response = elasticSearchClient.search(request, RequestOptions.DEFAULT);

            Suggest suggest = response.getSuggest();
            CompletionSuggestion suggestions = suggest.getSuggestion("suggestions");
            List<CompletionSuggestion.Entry.Option> options = suggestions.getOptions();
            for (CompletionSuggestion.Entry.Option option : options) {
                System.out.println(option.getText().string());
            }
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }
}

```

## 数据同步

# springboot IOC AOP

1.基于xml文件的IOC

xml文件：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 加载 application-dev.yaml 中的属性 -->
    <context:property-placeholder location="classpath:application-dev.yaml"/>
	//class是配置类的类路径，即特殊引用
    //id是这个配置类的名称
    <bean id="config" class="com.web.fakeq.XmlToBean.SpringIOC_Tes">
        <property name="host" value="localhost"></property>
        <property name="port" value="8080"></property>
        <property name="password" value="123"></property>
    </bean>
</beans>
```

配置类

```java

@Data
@Component
public class SpringIOC_Tes {
    
    private String host;

    private String password;

    private int port;

}

public class IOC_Test {
    public static void main(String[] args) {
        //基于xml加载IOC
        ClassPathXmlApplicationContext classPathXmlApplicationContext = new ClassPathXmlApplicationContext("spring.xml");
        SpringIOC_Tes springIOCTes = (SpringIOC_Tes) classPathXmlApplicationContext.getBean("config");
        System.out.println("基于xml的ioc加载："+springIOCTes);
    }
}
```

2.基于注解的ioc

2.1.配置类

配置类：

```java
@Configuration // 表示这是一个配置类
public class IOC_Config {
    @Bean // 
    public SpringIOC_Tes iocBeanByConfig(){
        SpringIOC_Tes springIOCTes = new SpringIOC_Tes();
        springIOCTes.setHost("localhost");
        springIOCTes.setPort(8080);
        springIOCTes.setPassword("123");
        return springIOCTes;
    }
}

// 使用配置类的类名加载
public class IOC_Test {
    public static void main(String[] args) {
        //配置类名加载
        ApplicationContext applicationContext = new AnnotationConfigApplicationContext(IOC_Config.class);
        //包扫描加载
        ApplicationContext applicationContext = new AnnotationConfigApplicationContext("com.web.fakeq.XmlToBean");
        System.out.println(applicationContext.getBean("iocBeanByConfig"));
    }
}

```

2.2 扫包+注解

加载的类：

```java
@Data
@Component
public class SpringIOC_Tes {
    @Value("${db.host}")
    private String host;
    @Value("${db.port}")
    private String password;
    @Value("${db.password}")
    private int port;

}
```

```java
@SpringBootTest
public class IOC_Test {
    @Test
    public void main() {
        ApplicationContext applicationContext = new AnnotationConfigApplicationContext("com.web.fakeq.testBean");
        System.out.println(applicationContext.getBean(SpringIOC_Tes.class));
	  }
}

```

# JAVA IO 知识：

字节输入输出流，字节输入输出缓冲流

```java
public class FileTes {
    public static void main(String[] args) throws IOException {
        //
        searchStrNum("C:\\Users\\goodf\\Desktop\\leetcode\\LootCode\\src\\main\\java\\FileStreamReview\\filestu.txt",
                "C:\\Users\\goodf\\Desktop\\leetcode\\LootCode\\src\\main\\java\\FileStreamReview\\filestucopy.txt");
    }
    public static  void searchStrNum(String filePath,String str) throws IOException {
		//字节输入流
        FileInputStream fileReader = new FileInputStream(new File(filePath));
        //字节输入缓冲
        BufferedInputStream bufferIn = new BufferedInputStream(fileReader);
        //字节输出流
        FileOutputStream fileWriter = new FileOutputStream(new File(str));
        //字节输出缓冲
        BufferedOutputStream bufferOut = new BufferedOutputStream(fileWriter);
		
        byte[] bytes = new byte[1024];
        int length;
        while ((length = bufferIn.read(bytes))!= -1){
            System.out.println(new String(bytes,0,length));
            bufferOut.write(bytes,0,length);
        }
	
        System.out.println("完成");
        //关闭包装流原始流就会自己关闭
        bufferOut.close();
        bufferIn.close();
    }
}
```

字符输入输出流

```
public class FileTes {
    public static void main(String[] args) throws IOException {
        //
        searchStrNum("C:\\Users\\goodf\\Desktop\\leetcode\\LootCode\\src\\main\\java\\FileStreamReview\\filestu.txt",
                "C:\\Users\\goodf\\Desktop\\leetcode\\LootCode\\src\\main\\java\\FileStreamReview\\filestucopy.txt");
    }
    public static  void searchStrNum(String filePath,String str) throws IOException {

        FileReader fileReader = new FileReader(new File(filePath));
 
        FileWriter fileWriter = new FileWriter(new File(str));
        char [] buffer = new char[5];
        int len;
        while(( len= bufferedReader.read(buffer)) != -1){
            bufferedWriter.write(buffer,0,len);
        }
        System.out.println("完成");
        fileWriter.flush();
        fileWriter.close();
        fileReader.close();
    }
}

```





# 📦 StringRedisTemplate 常用 API 总结

## 📌 一、String 操作（opsForValue）

```
StringRedisTemplate redisTemplate = new StringRedisTemplate();
ValueOperations<String, String> ops = redisTemplate.opsForValue();
```

------

### 1️⃣ 基本操作

```
ops.set("key", "value");                 // 设置值
ops.get("key");                         // 获取值
ops.getAndSet("key", "newValue");       // 先获取再设置
ops.setIfAbsent("key", "value");        // 不存在才设置（SETNX）
ops.setIfPresent("key", "value");       // 存在才设置
```

------

### 2️⃣ 过期时间

```
ops.set("key", "value", 10, TimeUnit.SECONDS); // 设置值 + 过期时间
redisTemplate.expire("key", 10, TimeUnit.SECONDS); // 单独设置过期
redisTemplate.getExpire("key"); // 获取剩余时间
```

------

### 3️⃣ 数值操作（计数器）

```
ops.increment("count");        // +1
ops.increment("count", 5);     // +5
ops.decrement("count");        // -1
ops.decrement("count", 3);     // -3
```

📌 常见场景：

- 点赞数
- 浏览量
- 限流计数

------

### 4️⃣ 字符串扩展

```
ops.append("key", "suffix");   // 追加
ops.size("key");               // 长度
ops.get("key", 0, 5);          // 截取子串
```

------

### 5️⃣ 批量操作

```
ops.multiSet(Map.of("k1", "v1", "k2", "v2"));  
ops.multiGet(List.of("k1", "k2"));             
ops.multiSetIfAbsent(map);      
```

## 📌 二、Set 操作（opsForSet）

```
SetOperations<String, String> setOps = redisTemplate.opsForSet();
```

------

### 1️⃣ 添加 / 删除

```
setOps.add("set1", "a", "b", "c");   
setOps.remove("set1", "a");          
```

------

### 2️⃣ 查询

```
setOps.members("set1");       
setOps.isMember("set1", "a"); 
setOps.size("set1");          
```

------

### 3️⃣ 随机操作

```
setOps.randomMember("set1");             
setOps.randomMembers("set1", 2);         
setOps.distinctRandomMembers("set1", 2); 
setOps.pop("set1");                      
```

------

### 4️⃣ 集合运算 ⭐（重点）

```
setOps.intersect("set1", "set2");   
setOps.union("set1", "set2");       
setOps.difference("set1", "set2");  
```

#### 📌 存储结果

```
setOps.intersectAndStore("set1", "set2", "dest");
setOps.unionAndStore("set1", "set2", "dest");
setOps.differenceAndStore("set1", "set2", "dest");
```

------

### 5️⃣ 多集合操作

```
setOps.union(List.of("set1", "set2"));       
setOps.intersect(List.of("set1", "set2"));   
```